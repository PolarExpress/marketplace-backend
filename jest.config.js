/** @type {import('ts-jest').JestConfigWithTsJest} */

// THIS FILE SHOULD BE REPLACED BY jest.config.json
// jest.config.js has some weird behavior sometimes

const config = {
    clearMocks: true,
    preset: "ts-jest",
    testEnvironment: "node",
}

module.exports = config
